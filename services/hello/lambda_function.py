import json

def lambda_handler(event, context):
    print(f'Hello World!')
    return {
        "statusCode": 200,
        "body": json.dumps({
            "message": "hello world",
            # "location": ip.text.replace("\n", "")
        }),
    }